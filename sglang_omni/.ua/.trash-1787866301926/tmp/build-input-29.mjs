import fs from "node:fs";
const raw = JSON.parse(fs.readFileSync("sglang_omni/.ua/intermediate/batches.json", "utf8"));
const batch = (Array.isArray(raw) ? raw : raw.batches).find((item) => item.batchIndex === 29);
if (!batch) throw new Error("找不到 batchIndex 29");
const output = { projectRoot: "C:\\Users\\erlic\\Documents\\sgl-omni\\sglang-omni\\sglang_omni", batchFiles: batch.files, batchImportData: batch.batchImportData, neighborMap: batch.neighborMap };
const json = JSON.stringify(output, null, 2) + "\n";
console.log("*** Begin Patch");
console.log("*** Add File: sglang_omni/.ua/tmp/ua-file-analyzer-input-29.json");
for (const line of json.split("\n").slice(0, -1)) console.log(`+${line}`);
console.log("*** End Patch");
